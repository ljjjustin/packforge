description "Ops Alert API"

start on stopped rc RUNLEVEL=[2345]
stop on runlevel [S016]

respawn

exec su -s /bin/sh -c "exec /usr/bin/alert-api --config-file /etc/alerting/alerting.conf --logfile /var/log/alerting/alerting.log" alerting 
