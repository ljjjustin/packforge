description "Ops Account API"

start on stopped rc RUNLEVEL=[2345]
stop on runlevel [S016]

respawn

exec su -s /bin/sh -c "exec /usr/bin/account-api --config-file /etc/accounting/accounting.conf --logfile /var/log/accounting/accounting.log" accounting
